npm install react-native-calendars

npm install axios

npx expo install expo-location

npm install express mongoose dotenv cors

npm install express cors dotenv mongoose @google/generative-ai

npm install mongoose

To run connection for mongodb cd into server and use: node index.js
